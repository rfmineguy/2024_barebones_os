#ifndef QUEUE_$T_H
#define QUEUE_$T_H
#include "SUPPORT_1"
#include "result.h"

define_result(queue_$T_dequeue, ^T, const char*);

typedef struct queue_$T_node queue_$T_node;
struct queue_$T_node {
  ^T val;
  queue_$T_node* next;
};

typedef struct {
  queue_$T_node* freelist;
  queue_$T_node *front, *back;
  int size;
} queue_$T;

queue_$T                queue_$T_create();
void                    queue_$T_free(queue_$T*);
void                    queue_$T_enqueue(queue_$T*, ^T);
result_queue_$T_dequeue queue_$T_dequeue(queue_$T*);

#endif
